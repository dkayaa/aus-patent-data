# Reshape raw Patent Search interim JSONL.GZ → analysis-ready interim JSONL.GZ.

paths:
  # Absolute or repo-relative. Created if missing (parents=True).
  # Expects part-*.jsonl.gz; writes mirrored part-*.jsonl.gz.
  input_dir: data/interim/patent_search
  output_dir: data/interim/patent_search_clean
  # Also writes {output_dir}/summary.json (counts + claims_parse_failures).
